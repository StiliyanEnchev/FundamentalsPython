received = [str(character) for character in input() if character != ' ']
dict = {}

for character in received:
  if character in dict:
    dict[character] += 1
  else: 
    dict[character] = 1

for key, value in dict.items():
  print(f'{key} -> {value}')
