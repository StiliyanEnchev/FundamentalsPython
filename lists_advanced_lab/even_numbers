string_with_nums = list(map(int, input().split(', ')))
list_of_indexes = []
for num in string_with_nums:
  if num % 2 == 0 and num != 0:
    list_of_indexes.append(string_with_nums.index(num))
print(list_of_indexes)
